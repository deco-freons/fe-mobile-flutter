enum TextFieldType {
  string,
  password,
  date,
}

enum ButtonType { primary, inverse, red }

enum TextButtonType {
  primary,
  secondary,
  tertiary,
}

enum AuthStatus {
  unknown,
  authenticated,
  unauthenticated,
}
