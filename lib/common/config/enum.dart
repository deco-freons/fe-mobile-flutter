enum TextFieldType {
  string,
  password,
  date,
}

enum ButtonType {
  primary,
  inverse,
}

enum TextButtonType {
  primary,
  secondary,
  tertiary,
}

enum AuthStatus {
  unknown,
  authenticated,
  unauthenticated,
}
